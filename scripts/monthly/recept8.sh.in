#!/bin/sh

if test -z "$JMARECEIPT_ENV" ; then
    JMARECEIPT_ENV="@jma-receipt-env@"
fi
if ! test -f "$JMARECEIPT_ENV"; then
    exit 0
fi

. $JMARECEIPT_ENV
#-------------------------------------------#
#    自賠責請求書用ファイル作成
#        CPCOMMONSHELL1.INC  
#        $1 SRYYM TERID SYSYMD
#        $2 SYOKBN
#           1:一括作成  2:個別作成
#        $3 RECEKBN
#           3:全体  
#        $4 エラーファイル名 
#        $5 JOBID 
#        $6 SHELLID
#        $7 県単用項目編集プログラム名（医保用）
#        $8 処理年月
#-------------------------------------------#
##      エラーファイル削除
	echo $#
	echo "echo " $4
        if  [ -e $4 ]; then
            rm  $4
        fi
        
        rm  /var/tmp/RECE4*

	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0405 -parameter $1$2$3$5$6    > /home/orca/recept8.log
	
	   
        if  [ -e $4 ]; then
            exit  
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0410 -parameter $1$5$6        >> /home/orca/recept8.log

        fi 

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0420 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0425 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0428 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0430 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0435 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

 
        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0440 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
           exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0450 -parameter $1$5$6        >> /home/orca/recept8.log

        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0460 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0465 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0467 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

 
        if  [ -e $4 ]; then
            exit 
        else 
	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0470 -parameter $1$5$6        >> /home/orca/recept8.log

        fi

        if  [ -e $4 ]; then
            exit 
        else 
           $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0485 -parameter $1$3$8$5$6    >> /home/orca/recept8.log
        fi
        
        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCBJOB -parameter JBE$5$6        >> /home/orca/recept8.log
	fi
	
        exit 
