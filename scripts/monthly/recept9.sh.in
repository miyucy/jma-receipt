#!/bin/sh

if test -z "$JMARECEIPT_ENV" ; then
    JMARECEIPT_ENV="@jma-receipt-env@"
fi
if ! test -f "$JMARECEIPT_ENV"; then
    exit 0
fi

. $JMARECEIPT_ENV

#-------------------------------------------#
#    医療請求書用ファイル（入院）作成
#
#        CPCOMMONSHELL1.INC  
#        $1 SRYYM TERID SYSYMD
#        $2 SYOKBN
#           1:一括作成  2:個別作成
#        $3 RECEKBN
#           0:全体  1:社保　2:国保
#        $4 エラーファイル名 
#        $5 JOBID 
#        $6 SHELLID
#        $7 県単用項目編集プログラム名
#-------------------------------------------#
##      エラーファイル削除
	echo $#
	echo "echo " $4
        if  [ -e $4 ]; then
            rm  $4
        fi
        
        rm  /var/tmp/RECE*
        

	$DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0600 -parameter $1$2$3$5$6
	
        if  [ -e $4 ]; then
            exit  
        else 
            $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0610 -parameter $1$2$3$5$6
	fi

        if  [ -e $4 ]; then
            exit  
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0620 -parameter $1$5$6
        fi

        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0630 -parameter $1$5$6
        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	   $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0640 -parameter $1$5$6
        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0650 -parameter $1$5$6
        fi

        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0655 -parameter $1$5$6
        fi

        if  [ -e $4 ]; then
            exit 
        else 
    	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0660 -parameter $1$5$6
        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0670 -parameter $1$5$6
        fi

        if  [ -e $4 ]; then
           exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0680 -parameter $1$5$6
        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0690 -parameter $1$5$6
        fi
 
        if  [ -e $4 ]; then
            exit 
        else 
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0700 -parameter $1$5$6
        fi
 
##        if  [ -e $4 ]; then
##            exit 
##        else 
##           if  [ $7 != "NOPG" ]; then
##	      $DBSTUB -dir "$LDDEFDIR"/directory -bd recept $7 -parameter $1$5$6
##	   fi
##        fi

 
        if  [ -e $4 ]; then
            exit 
        else 
            $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCR0710 -parameter $1$3$8$5$6
        fi
        
        if  [ -e $4 ]; then
            exit 
        else
	    $DBSTUB -dir "$LDDEFDIR"/directory -bd orcabt ORCBJOB -parameter JBE$5$6
	fi
	
        exit 
