#!/bin/sh

if test -z "$JMARECEIPT_ENV" ; then
    JMARECEIPT_ENV="@jma-receipt-env@"
fi
if ! test -f "$JMARECEIPT_ENV"; then
    exit 0
fi

. $JMARECEIPT_ENV

#-------------------------------------------#
#    収集データ作成
#        $1  TERMID
#        $2  HOSPID
#        $3  PREFNUM
#        $4  SRYYM
#        $5  BEDFLG (0:病床なし、1:病床あり)
#        $6  JOBID
#        $7  SHELLID
#        $8  UPLOAD (0:送信なし、1:送信あり、送信のみ)
#-------------------------------------------#

COLLECTG=collectg.csv
COLLECTN=collectn.csv
#                                collect-yyyymm.tar.gz (collectg.csv,collectn.csv)
LOGFILE=/home/orca/datacollect1.log
ERRFILE=/tmp/datacollecterror

DASUPLOAD=$SCRIPTSDIR/monthly/das-upload.sh

# エラーファイル削除
  if  [ -e $ERRFILE ]; then
    rm -f $ERRFILE
  fi

  rm -f /tmp/$COLLECTG
  rm -f /tmp/$COLLECTN
  rm -f /var/tmp/COLDT*

# ファイル送信のみ？
  if  [ $8 -ne '2' ]; then

#   外来分処理

    $DBSTUB -dir $LDDIRECTORY -bd orcabt ORCBC010 -parameter $1,$2,$3,$4,2,$6,$7,$ERRFILE 2>&1 | tee $LOGFILE
    if  [ -e $ERRFILE ]; then
	rm -f /var/tmp/COLDT*
    else
	$DBSTUB -dir $LDDIRECTORY -bd orcabt ORCBC020 -parameter $1,$4,$6,$7,/tmp/$COLLECTG,$ERRFILE 2>&1 | tee -a $LOGFILE
	if  [ -e $ERRFILE ]; then
	    rm -f /var/tmp/COLDT*
	    rm -f /tmp/$COLLECTG
	fi
    fi

    if  [ $5 -eq '1' ]; then

#   入院分処理

	$DBSTUB -dir $LDDIRECTORY -bd orcabt ORCBC010 -parameter $1,$2,$3,$4,1,$6,$7,$ERRFILE 2>&1 | tee -a $LOGFILE
	if  [ -e $ERRFILE ]; then
	    rm -f /var/tmp/COLDT*
	else
	    $DBSTUB -dir $LDDIRECTORY -bd orcabt ORCBC020 -parameter $1,$4,$6,$7,/tmp/$COLLECTN,$ERRFILE 2>&1 | tee -a $LOGFILE
	    if  [ -e $ERRFILE ]; then
		rm -f /var/tmp/COLDT*
		rm -f /tmp/$COLLECTN
	    fi
	fi
    fi

    if  [ $5 -eq '1' ]; then
	if  [ -e /tmp/$COLLECTG ] && [ -e /tmp/$COLLECTN ]; then
	    tar cvfz $DASDIR/collect-$4.tar.gz -C/ tmp/$COLLECTG tmp/$COLLECTN
	fi
	if  [ -e /tmp/$COLLECTG ] && [ ! -e /tmp/$COLLECTN ]; then
	    tar cvfz $DASDIR/collect-$4.tar.gz -C/ tmp/$COLLECTG
	fi
	if  [ ! -e /tmp/$COLLECTG ] && [ -e /tmp/$COLLECTN ]; then
	    tar cvfz $DASDIR/collect-$4.tar.gz -C/ tmp/$COLLECTN
	fi
    else
	if  [ -e /tmp/$COLLECTG ]; then
	    tar cvfz $DASDIR/collect-$4.tar.gz -C/ tmp/$COLLECTG
	fi
    fi

    rm -f /var/tmp/COLDT*

  fi

  $DBSTUB -dir $LDDIRECTORY -bd orcabt ORCBJOB -parameter JBE$6$7 2>&1 | tee -a $LOGFILE

  if  [ $8 -ne '0' ]; then
    echo "収集データ送信処理 起動" 2>&1 | tee -a $LOGFILE
    $DASUPLOAD &
  fi

  exit
