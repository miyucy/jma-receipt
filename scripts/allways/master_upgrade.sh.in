#!/bin/sh

if test -z "$JMARECEIPT_ENV"; then
    JMARECEIPT_ENV="@jma-receipt-env@"
fi
if ! test -f "$JMARECEIPT_ENV"; then
    exit 0
fi

. $JMARECEIPT_ENV

#標準マスタ更新
sh $SCRIPTSDIR/allways/master_standard_upgrade.sh $1

#ライセンスマスタ更新
sh $SCRIPTSDIR/allways/master_license_upgrade.sh $1
