#!/bin/sh

if test -z "$JMARECEIPT_ENV"; then
    JMARECEIPT_ENV="@jma-receipt-env@"
fi
if ! test -f "$JMARECEIPT_ENV"; then
    exit 0
fi

. $JMARECEIPT_ENV

HOME=/home/orca
PRGDIR=orca-prg@VERSION@

if ! [	-d	$HOME/$PRGDIR	];	then 
	echo "プログラムディレクトリが存在しません"
	exit
fi

#プログラム複写
	cp	$HOME/$PRGDIR/$1	 $PATCHDIR/$2
#
exit
